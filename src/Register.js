import React from 'react';

const Register = () => {
    return (
        <h1>Register</h1>
    )
}

export default Register

// Create a Register Form (complete with validation).

// The form has following fields (all are required fields):
    // Full name.
    // Email.
    // Password.
    // Phone number.
    // Profile picture.
    // Gender.
    // Address.
    // Country (List at least 4 countries).
    // Checkmark that user agree to Terms and Conditions.
